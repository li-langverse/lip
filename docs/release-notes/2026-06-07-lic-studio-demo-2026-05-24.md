# Release notes: 2026-06-07 — lic studio-demo-2026-05-24 pin

**Status:** Pending merge  
**Repo:** li-langverse/lip  
**PH / REQ:** ecosystem-upstream / meta-governance  
**Author:** code_implementer

---

## Summary

Bump `li-toolchain.toml` `lic_version` from `0.1.0` to upstream tag `studio-demo-2026-05-24` on `li-langverse/lic` (World Studio demo reel / HyperFrames release).

## Agent continuation

1. Read: `li-toolchain.toml` and [Renovate policy](../RENOVATE.md) for future `lic_version` bumps
2. Run: `./scripts/ci.sh` with sibling `lic` checkout at the pinned ref (CI workflow still tracks `main` until wired to the pin)
3. Then: when `lic` publishes the next semver tag, file or auto-open ecosystem-upstream issue via `ecosystem-upstream.yml`
4. Blocked on: none for this pin-only change

## Changed

| Area | What | Evidence |
|------|------|----------|
| Toolchain | `lic_version` → `studio-demo-2026-05-24` | `li-toolchain.toml` |
| Changelog | Unreleased entry | `CHANGELOG.md` |

## Not changed

- `lip` package manager, registry client, and publish scripts — unchanged
- `.github/workflows/ci.yml` lic checkout ref — still `main` (same as `lit`; pin is Renovate/downstream tracking)
- `lic` compiler source — no edits in this repo

## Breaking changes

None.

## Security

N/A — dependency pin metadata only.

## Performance

N/A.

## Downstream

| Repo | Action |
|------|--------|
| lit, lis, li-demo | Independent `lic_version` pins; bump when their ecosystem-upstream issues open |

## CHANGELOG entry

```markdown
### Changed
- Ecosystem: bump `lic_version` pin to `studio-demo-2026-05-24` (upstream `li-langverse/lic` release).
```
